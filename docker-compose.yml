services:
  bridge:
    build: .
    image: http-bridge:latest
    container_name: http-bridge
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-8080}:8080"
    environment:
      PORT: "8080"
      API_KEY: "${API_KEY:?set API_KEY in .env}"
